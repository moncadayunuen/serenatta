{
  "name": "serenatta",
  "version": "1.0.0",
  "description": "layout",
  "main": "js/app.js",
  "scripts": {
    "browser": "browser-sync start --server --files \"*.html, css/**/*.css, js/**/*.js\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.27.10"
  },
  "dependencies": {
    "@chenfengyuan/datepicker": "^1.0.9",
    "jquery-timepicker": "^1.3.3"
  }
}
